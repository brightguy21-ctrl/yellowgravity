build:
  gpu: true               # Use GPU (important for MedGemma)
  python_version: "3.10"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
  python_packages:
    - torch==2.0.1
    - torchvision==0.15.2
    - torchaudio==2.0.2
    - transformers==4.35.0
    - gradio==4.16.0
    - pillow==10.0.0
    - requests==2.31.0
    - huggingface-hub==0.20.2
    - bitsandbytes==0.41.2  # for 4-bit quantization (helps with memory)
    - accelerate==0.25.0

predict: "predict.py:Predictor"
